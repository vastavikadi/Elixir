# All of the code is executed inside something called actor. Think of it as a isolated box and it gets some kind of messages and simply going to give the response after processing those messages.
# The actor is inside of something called processes. Each process is like a lightweight thread that can run concurrently with other processes. 
